#include <stdio.h>
float convertn temp (float celsius);

int main()
{
    float far = convert temp (37);
    printf("far %f " far);

    return 0;
}
   float convert temp ( float celsius){
       float for = celsius *(9.0/5.0)+32;
       return for ;
   }
